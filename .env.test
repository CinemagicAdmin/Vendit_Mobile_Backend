# =============================================================================
# Test Environment Configuration
# =============================================================================
# This file is used for running tests with `npm test`
# =============================================================================

NODE_ENV=test

# Supabase (use mock or local instance)
SUPABASE_URL=http://localhost:54321
SUPABASE_SERVICE_KEY=test-key
SUPABASE_ANON_KEY=test-anon-key

# Redis (use in-memory for tests)
REDIS_URL=memory://local

# JWT Secrets (test only)
JWT_ACCESS_SECRET=test-access-secret-for-testing-purposes-only
JWT_REFRESH_SECRET=test-refresh-secret-for-testing-purposes-only
CSRF_SECRET=test-csrf-secret-for-testing

# Session
COOKIE_SECRET=test-cookie-secret
COOKIE_DOMAIN=localhost

# Disable external services in tests
SMS_DRIVER=log
ALLOW_SMS_FALLBACK=true

# Firebase (optional for tests)
FIREBASE_PROJECT_ID=test-project
FIREBASE_CLIENT_EMAIL=test@test.com
FIREBASE_PRIVATE_KEY="test-key"

# Tap Payments (test mode)
TAP_SECRET_KEY=sk_test_TESTING
TAP_PUBLIC_KEY=pk_test_TEST ING
TAP_DEFAULT_CURRENCY=KWD

# Disable Sentry in tests
# SENTRY_DSN=

# Loyalty & Referral 
LOYALTY_BASE_RATE=10
REFERRAL_INVITER_POINTS=250
REFERRAL_INVITEE_POINTS=250
